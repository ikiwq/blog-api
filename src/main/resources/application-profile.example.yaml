spring:
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto:
    properties:
      hibernate:
        dialect:

app:
  upload-folder:
  domain:
  admin:
    credentials:
      username:
      password: